# VaulTLS Docker Environment Configuration

# Database Configuration
# Database encryption secret (generate with: openssl rand -hex 32)
VAULTLS_DB_SECRET=

# API Configuration
# API secret for JWT tokens and authentication (auto-generated if not set)
VAULTLS_API_SECRET=

# Network Configuration
# Backend API port (inside container)
VAULTLS_BACKEND_PORT=8000
# Frontend web server port (inside container)
VAULTLS_FRONTEND_PORT=4000
# External ports (change these to run multiple instances)
HTTP_PORT=80
BACKEND_API_PORT=8000

# Development Configuration
# Set to true to run tests during build (increases build time significantly)
RUN_TESTS=false

# Performance Configuration
# Rocket framework configuration
ROCKET_ADDRESS=0.0.0.0
ROCKET_PORT=8000
ROCKET_KEEP_ALIVE=30
ROCKET_WORKERS=4

# Production SSL/HTTPS Configuration (when ready)
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/private.key

# Backup Configuration
# BACKUP_SCHEDULE=0 0 * * *
# BACKUP_RETENTION_DAYS=30
